1. Navigate to the this folder:

2. Modify index.html file

here is need to change server ip address
63 line ---    (m) => `http://127.0.0.1:8080/modules/${m}.zip`

3. Run the `live-server` in the this folder

```sh
npx live-server
```

\* [Nodejs](https://nodejs.org/en/) must be installed to run the `npx live-server` command

Open [localhost:8080](http://localhost:8080) and start clicking :)